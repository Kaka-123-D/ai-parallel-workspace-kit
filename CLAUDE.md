# CLAUDE.md

Đọc `AGENTS.md` trước. Skill nằm ở `.claude/skills` (symlink sang `.agents/skills`), chọn theo `description` của từng skill.
Trong `.claude/` chỉ có symlink `skills`, `rules` trỏ sang `.agents/` và `settings.json` (allowlist quyền).
